(function(window){
    window["env"] = window["env"] || {};

    // Environment variables
    window["env"]["serverURL"] = "${SERVER_URL}";
    window["env"]["idcajero"] = "${ID_CAJERO}";
})(this);
//inserted in index.html